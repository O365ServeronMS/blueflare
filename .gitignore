node_modules/
.next/
out/
dist/
build/

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
*.log

backups/
*.bak
*.bak.*
*.tmp
*.swp
tmp/

# Local editor/workspace state
.vscode/
scratch/

# Local visual QA evidence (large binary screenshots)
docs/assets/

# TypeScript incremental cache
*.tsbuildinfo

# Local assistant scratch notes. CLAUDE.md itself is tracked on purpose:
# it is the onboarding document, so it must survive a fresh clone.
claude.local.md

# Local Blueflare signed-image cache
backend/data/images/

# Claude Code local state and worktrees (multi-GB if left untracked)
.claude/
.codex/

# Astro content-collection cache (legacy stack)
.astro/
